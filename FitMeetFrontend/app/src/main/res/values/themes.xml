<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.VideoCall" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorControlNormal">@color/white</item>
    </style>

    <style name="Theme.VideoCall" parent="Base.Theme.VideoCall" />

    <style name="AppTheme.Menu" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <item name="colorControlNormal">@color/white</item>
    </style>
    <style name="ToolbarTheme" parent="@style/ThemeOverlay.AppCompat.ActionBar">
    <item name="colorControlNormal">@color/colorWhite</item>
    </style>
</resources>