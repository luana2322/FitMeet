<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/white"/> <!-- Màu nền, bạn có thể thay đổi -->
    <stroke
        android:width="2dp"
        android:color="@color/main_blue"/> <!-- Thay đổi màu viền nếu cần -->
</shape>